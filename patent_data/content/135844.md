# A data processor with a branch target instruction storage.

## Abstract
In order to allow fast execution of branch instructions even with the possibility of branching to different target in structions from the same branch instruction, a target instruc tion storage 102 provides the target instruction in response to the branch instruction address and address data associated with a target instruction address.