# Inquiry system for detecting a selected object.

## Abstract
An inquiry system is provided for detecting the location of a selected object. The system comprises an answering device having its own identifying address which responds to a detect ing command. An answering device 2 is provided on each ob ject to be selected. The detecting command is transmitted from a controller device 5 which communicates with the answer ing devices through the use of an address.