# STATIC RAM HAVING A FLASH CLEAR FUNCTION

## Abstract
A static RAM having a flip flop cell employs a circuit for simultaneously clearing all cells to a logic zero, in which one of two ground lines is raised to VCC, thereby forcing one node of the flip flop high.