# DISPLAY ARCHITECTURE HAVING VARIABLE DATA WIDTH

## Abstract
The display architecture supports a variable, selectable number of bits per chip and a variable, selectable segment width. The architecture comprises a plurality of dynamic memory chips and a function generator. Each of the memory chips includes at least two data islands wherein each data island has its own data in out line, chip select and increment bit supplied by the function generator. The function gener ator receives a starting address X