# Apparatus for generating an instantaneous FIFO binary arithmetic code string, apparatus for reconstructing a binary symbol string from such a code string, and a method for recursively encoding, and a method for recursively decoding, an instantaneous FIFO binary arithmetic number string.